
import math
'''
r = float(input("What is the radius of your circle?: "))

circumference = 2 * math.pi * r
print(f"Your circle's circumference is: {round(circumference, 2)}cm") 
 #for the round function, it is (variable, decimal place you want to round to)
 
#---------------------------------------------------------------------------------

r = float(input("What is the radius of your circle?: "))

Area = math.pi * pow(r, 2)
print(f"The area of your circle is {round(Area, 1)}cm^2")

#---------------------------------------------------------------------------------

a = float(input("What is the length of the first side?: "))
b = float(input("What is the length of the second side?: "))

c = math.sqrt(pow(a,2) + pow(b,2))
print(f"The hypotenuse of your triangle is {round(c,2)}cm")
#---------------------------------------------------------------------------------
'''